# Codex活用拡張案

## 1. 概要

現在のシステムでは、Codexを主軸としてコードレビューを実施し、レビュー結果の登録、修正依頼、修正結果の確認までを行っている。

この仕組みはコードレビュー以外にも展開可能であり、今後の拡張候補として以下の3種類を検討する。

* 調査
* 実装
* 仕様書更新

これらはいずれも、Codexが定期的にタスク一覧をスキャンし、依頼されたタスクを実行して結果をシステムへ登録する現在の仕組みを流用できる。

---

## 2. 検討対象

| 種別        | 目的                             | ユーザーからの依頼例                                                                | Codexの主な処理                             | システムに残す結果                                             | 特徴                 |
| --------- | ------------------------------ | ------------------------------------------------------------------------- | -------------------------------------- | ----------------------------------------------------- | ------------------ |
| **調査**    | コードベースを解析し、質問、影響範囲、現状仕様などに回答する | 「この処理はどこで実装されている？」<br>「Customer.Id変更時の影響範囲を調べて」<br>「ユーザー削除時に関連データも削除される？」 | リポジトリ横断検索、呼び出し関係解析、依存関係解析、DB/API/UIの追跡 | 結論、根拠となるファイル・行、影響範囲、関連コード、Mermaid/PlantUML図、必要に応じて改善案 | コードを変更しない読み取り系タスク  |
| **実装**    | ユーザーの要求をもとに実際にコードを変更する         | 「CSVエクスポート機能を追加して」<br>「この不具合を修正して」<br>「この処理をリファクタリングして」                   | 現状調査、実装方針決定、コード変更、必要なテスト追加・実行          | 変更概要、変更ファイル、差分、テスト結果、注意事項、修正前後の図や画像                   | コードを変更する書き込み系タスク   |
| **仕様書更新** | 実装と仕様書を一致させ、ドキュメントを保守する        | 「この機能の仕様書を作って」<br>「実装変更をMkDocsへ反映して」<br>「コードと仕様書の差異を調べて修正して」              | コード解析、既存仕様書解析、差分検出、Markdownや図の生成・更新    | 更新した仕様書、変更箇所、コードとの対応関係、Mermaid/PlantUML、仕様変更理由        | コードとドキュメントを同期するタスク |

---

## 3. 調査

### 3.1 目的

コードベースに対する各種調査をCodexへ依頼し、その結果をシステム上に蓄積する。

調査という1つのタスク種別の中で、以下のような用途を扱う。

* 機能や処理の実装箇所の調査
* 変更時の影響範囲調査
* 不具合や挙動の原因調査
* 現在の実装仕様の確認
* コードベースに対する質問

### 3.2 依頼例

```text
この処理はどこで実装されている？
```

```text
Customer.IdをstringからGuidへ変更した場合の影響範囲を調査して。
```

```text
ユーザー削除時に関連するデータがすべて削除されるか調査して。
```

```text
このシステムではパスワードをどこでハッシュ化している？
```

### 3.3 Codexの処理

Codexは依頼内容に応じて、必要な調査方法を自身で判断する。

主な処理として以下を想定する。

* リポジトリ全体の検索
* 呼び出し関係の追跡
* クラスやモジュール間の依存関係解析
* APIの追跡
* DBアクセスの追跡
* UIからバックエンドまでの処理フロー追跡

### 3.4 登録する結果

調査結果として、必要に応じて以下をシステムへ登録する。

* 調査結果の結論
* 根拠となるファイル
* 根拠となるコード位置
* 関連するクラス、API、DB等
* 影響範囲
* 注意点
* 改善案
* Mermaidによる図
* PlantUMLによる図
* その他必要な画像

調査種別を細かく分割するのではなく、基本的には「調査」という1種類として扱い、必要であればタグやカテゴリによって分類する。

例：

```text
impact-analysis
bug-analysis
specification-analysis
code-question
```

---

## 4. 実装

### 4.1 目的

ユーザーからの要求をCodexへタスクとして渡し、実際のコード変更まで実施させる。

現在のコードレビューにおける、

```text
レビュー指摘
↓
Codexへの修正依頼
↓
コード修正
↓
人間による確認
```

という流れを、レビュー以外の一般的な実装タスクにも拡張する。

### 4.2 依頼例

```text
CSVエクスポート機能を追加して。
```

```text
この不具合を修正して。
```

```text
この処理をリファクタリングして。
```

### 4.3 Codexの処理

主な処理として以下を想定する。

1. 現在の実装を調査する
2. 変更対象と影響範囲を確認する
3. 実装方針を決定する
4. コードを変更する
5. 必要に応じてテストを追加する
6. テストを実行する
7. 結果をシステムへ登録する

### 4.4 登録する結果

実装結果として、以下の情報を残す。

* 実装内容の概要
* 変更したファイル
* 変更内容
* テスト結果
* 注意事項
* 人間が確認すべき点
* 必要に応じて修正前後の画像
* 必要に応じてMermaid / PlantUMLによる説明図

---

## 5. 仕様書更新

### 5.1 目的

コードと仕様書の差異をCodexに確認させ、実装内容に合わせて仕様書を作成または更新する。

特にMarkdownやMkDocs等で管理している仕様書との連携を想定する。

### 5.2 依頼例

```text
この機能の仕様書を作成して。
```

```text
今回の実装変更をMkDocsの仕様書へ反映して。
```

```text
現在のコードと仕様書に差異がないか調査し、必要であれば仕様書を修正して。
```

### 5.3 Codexの処理

主な処理として以下を想定する。

1. 対象コードを解析する
2. 既存仕様書を解析する
3. コードと仕様書を比較する
4. 差分を特定する
5. Markdown等の仕様書を生成または修正する
6. 必要なMermaid / PlantUML図を生成する
7. 更新結果をシステムへ登録する

### 5.4 登録する結果

仕様書更新結果として、以下の情報を残す。

* 更新した仕様書
* 更新箇所
* 変更理由
* 対応するコード
* コードと仕様書の差分
* Mermaidによる図
* PlantUMLによる図

---

## 6. タスク種別

システム上では、以下のようなタスク種別として扱うことを想定する。

| Task Type        | 表示名   | Codexの基本動作                   |
| ---------------- | ----- | ---------------------------- |
| `Investigation`  | 調査    | コードを読み取り、解析して結果を回答する         |
| `Implementation` | 実装    | 調査後にコードを変更し、必要なテストを実施する      |
| `Documentation`  | 仕様書更新 | コードと仕様書を解析し、ドキュメントを作成または更新する |

---

## 7. 共通ワークフロー

3種類とも、基本的には現在のCodex運用と同様のワークフローを利用する。

```text
ユーザーがタスク登録
        ↓
Codex実行待ち
        ↓
Codexが定期スキャン
        ↓
対象タスクを検出
        ↓
Codexがタスク実行
        ↓
結果をシステムへ登録
        ↓
人間が結果を確認
        ↓
完了
```

必要に応じて、人間による確認後に追加作業をCodexへ再依頼できる構造とする。

```text
Codex実行
    ↓
結果登録
    ↓
人間確認
    ↓
追加依頼
    ↓
Codex再実行
```

---

## 8. 今後の検討事項

今後、現在のコードレビュー機能に加えて、以下の3種類をシステムへ追加することを検討する。

1. 調査
2. 実装
3. 仕様書更新

特に「調査」については用途ごとにタスク種別を細分化せず、Codexが依頼内容から必要な調査方法を判断できる構造を基本とする。

これにより、現在のコードレビュー中心のシステムを、Codexへ各種Engineering Taskを依頼し、その実行結果を蓄積・確認するためのシステムへ拡張できる。
